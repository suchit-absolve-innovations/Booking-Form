import { ChunkPipePipe } from './chunk-pipe.pipe';

describe('ChunkPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ChunkPipePipe();
    expect(pipe).toBeTruthy();
  });
});
